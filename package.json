{
  "name": "obsidian-daily-recall",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild main.ts --bundle --external:obsidian --format=cjs --outfile=main.js --watch",
    "build": "esbuild main.ts --bundle --external:obsidian --format=cjs --outfile=main.js",
    "package": "mkdir -p dist && cp main.js manifest.json dist/ && cd dist && zip -r obsidian-daily-recall-1.0.0.zip *"
  },
  "keywords": ["obsidian", "plugin", "daily-notes"],
  "author": "",
  "license": "MIT",
  "description": "Seamlessly continue your daily notes with content from previous entries",
  "devDependencies": {
    "obsidian": "^1.8.7",
    "typescript": "^5.8.3",
    "esbuild": "^0.25.0"
  }
}
